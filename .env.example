# AgentOS phase-1 local environment
AGENTOS_ENV=development
AGENTOS_DATA_DIR=.agentos
AGENTOS_APPROVAL_MODE=edit-with-approval

# Optional: enables real AgentOS command/chat responses through Electron main.
# Renderer never receives this key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
